#ifndef PLUGIN_HPP
#define PLUGIN_HPP

static void iterate_function_body(tree);
static void and_to_or_mutator(tree);
static void or_to_and_mutator(tree);
static void lt_to_gt(tree);
static void gt_to_lt(tree);
#endif